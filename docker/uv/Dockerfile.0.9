# uv 0.9 (max tested version)
FROM golang:1.25-alpine

RUN apk add --no-cache python3 py3-pip git
RUN pip3 install uv==0.9.7 --break-system-packages

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .

CMD ["go", "test", "./...", "-v"]
