# Mix/Elixir 1.12+ (minimum supported)
FROM golang:1.25-alpine

# Install Elixir and Erlang
RUN apk add --no-cache elixir git

# Install Hex
RUN mix local.hex --force

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .

CMD ["go", "test", "./...", "-v"]
