# Poetry 1.2+ (minimum supported)
FROM golang:1.25-alpine

RUN apk add --no-cache python3 py3-pip git

# Install Poetry 1.2.0
RUN pip3 install --break-system-packages poetry==1.2.0

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .

CMD ["go", "test", "./...", "-v"]
